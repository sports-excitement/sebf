# =============================================================================
# Sports Excitement Core - Testing Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Application Settings (Testing)
# -----------------------------------------------------------------------------
APP_NAME="Sports Excitement Core - Test"
APP_VERSION="1.0.0"
APP_ENV=testing
APP_KEY=RfM84p/o89It2fKdB+E4r9TrYwah4eww+qtkCbSuCK0=
APP_DEBUG=false
APP_URL=http://localhost:3001

# Server Configuration
PORT=3001
HOST=0.0.0.0

# Debug and Logging (Minimal for testing)
DEBUG=false
LOG_LEVEL=error
LOG_CHANNEL=stack

# -----------------------------------------------------------------------------
# Security Configuration (Testing)
# -----------------------------------------------------------------------------
# JWT Authentication (Test keys - pre-generated for consistency)
JWT_SECRET=321be65f5633e55dfc63720a8447279ffc0a41fb1b39a6ee651b4d0414499b3336886ffceb2cc10430c08fdca2a92852c2da0836c0fefdbf140589ca9700e6af
JWT_EXPIRES_IN=1h
JWT_ALGORITHM=HS256
JWT_ISSUER=sports-excitement-core-test
JWT_AUDIENCE=sports-excitement-app-test

# Session Management (Test keys - pre-generated for consistency)
SESSION_SECRET=c60e4874aaf14504e35f02f5d74cf167323670ca0f787217bf5f7d3c66776a9a003daf757421709c812afa7dfcb0b6ed399d7ef147ad0fe3f8c71827b8caa0bd
SESSION_NAME=se_test_session
SESSION_MAX_AGE=3600000
SESSION_SAME_SITE=strict
SESSION_SAVE_UNINITIALIZED=false
SESSION_RESAVE=false

# Health Check API Key (Test key)
HEALTH_CHECK_API_KEY=test-health-check-api-key-12345

# API Security (Relaxed for testing)
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX=1000
API_RATE_LIMIT_MESSAGE="Too many requests, please try again later"

# -----------------------------------------------------------------------------
# Service Configuration (Testing - Most services disabled)
# -----------------------------------------------------------------------------
# Database (Required - uses SQLite for testing)
DATABASE_ENABLED=true

# Redis (Enabled for session testing)
REDIS_ENABLED=true

# Typesense (Disabled for testing)
TYPESENSE_ENABLED=false

# MinIO (Disabled for testing)
MINIO_ENABLED=false

# Supabase (Disabled for testing)
SUPABASE_ENABLED=false

# Firebase (Disabled for testing) 
FIREBASE_ENABLED=false

# Server-Sent Events (Enabled with reduced limits)
SSE_ENABLED=true

# Memory Monitoring (Disabled for testing)
MEMORY_ENABLED=false

# Email Service (Disabled for testing)
EMAIL_ENABLED=false

# -----------------------------------------------------------------------------
# Database Configuration (Testing - SQLite for tests)
# -----------------------------------------------------------------------------
# Test Database (SQLite for all tests - faster and isolated)
DATABASE_URL="file:./prisma/testing/test.db"

# PostgreSQL Database (Disabled for testing - uncomment if needed for integration tests)
# DATABASE_URL="postgresql://test_user:test_password@localhost:5432/sports_excitement_test?schema=public"
TEST_DATABASE_URL="file:./prisma/testing/test.db"

# Database Connection Settings (Minimal for testing - SQLite)
DB_CONNECTION=sqlite
DB_DATABASE=./prisma/test.db
# PostgreSQL settings (disabled for testing)
# DB_HOST=localhost
# DB_PORT=5432
# DB_USERNAME=test_user
# DB_PASSWORD=test_password
# DB_SCHEMA=public
DB_MAX_CONNECTIONS=1
DB_CONNECTION_TIMEOUT=2000
DB_IDLE_TIMEOUT=10000

# -----------------------------------------------------------------------------
# Redis Configuration (Testing - Separate Database)
# -----------------------------------------------------------------------------
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DATABASE=1

# Redis Connection Pool (Test settings - faster timeouts)
REDIS_CONNECT_TIMEOUT=2000
REDIS_COMMAND_TIMEOUT=2000
REDIS_RETRY_DELAY=50
REDIS_MAX_RETRIES=2
REDIS_FAMILY=4
REDIS_KEY_PREFIX=sports-excitement-test:

# Redis Fallback (Always fallback in tests)
REDIS_FALLBACK_TO_MEMORY=true

# -----------------------------------------------------------------------------
# Typesense Configuration (Disabled - Test Values)
# -----------------------------------------------------------------------------
TYPESENSE_HOST=localhost
TYPESENSE_PORT=8108
TYPESENSE_PROTOCOL=http
TYPESENSE_API_KEY=test-typesense-api-key

# Typesense Settings (Test values)
TYPESENSE_CONNECTION_TIMEOUT=2
TYPESENSE_HEALTHCHECK_INTERVAL=10
TYPESENSE_NUM_RETRIES=1
TYPESENSE_RETRY_INTERVAL=0.1
TYPESENSE_LOG_LEVEL=error

# Typesense Collections (Test collections)
TYPESENSE_USERS_COLLECTION=test_users
TYPESENSE_LOGS_COLLECTION=test_system_logs
TYPESENSE_ANALYTICS_COLLECTION=test_analytics

# -----------------------------------------------------------------------------
# MinIO Configuration (Disabled - Test Values)
# -----------------------------------------------------------------------------
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_USE_SSL=false
MINIO_ACCESS_KEY=test-access-key
MINIO_SECRET_KEY=test-secret-key

# MinIO Settings (Test values)
MINIO_BUCKET=test-bucket
MINIO_REGION=us-east-1
MINIO_PART_SIZE=16
MINIO_MAX_RETRIES=1
MINIO_RETRY_DELAY=100

# -----------------------------------------------------------------------------
# Supabase Configuration (Disabled - Test Values)
# -----------------------------------------------------------------------------
SUPABASE_URL=https://test.supabase.co
SUPABASE_KEY=test-anon-key
SUPABASE_SERVICE_ROLE_KEY=test-service-key

# Supabase Settings (Test values)
SUPABASE_AUTH_ENABLED=false
SUPABASE_STORAGE_BUCKET=test-files

# -----------------------------------------------------------------------------
# Firebase Configuration (Disabled - Test Values)
# -----------------------------------------------------------------------------
FIREBASE_SERVICE_ACCOUNT_KEY=test/firebase-service-account-test.json
FIREBASE_DATABASE_URL=https://test-project-default-rtdb.firebaseio.com/
FIREBASE_STORAGE_BUCKET=test-project.appspot.com

# Firebase Collections (Test collections)
FIREBASE_NOTIFICATIONS_COLLECTION=test_notifications
FIREBASE_DEVICE_TOKENS_COLLECTION=test_device_tokens
FIREBASE_ANALYTICS_COLLECTION=test_analytics
FIREBASE_USER_PROFILES_COLLECTION=test_user_profiles

# -----------------------------------------------------------------------------
# Email Configuration (Disabled - Test Values)
# -----------------------------------------------------------------------------
EMAIL_HOST=smtp.test.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=test@test.com
EMAIL_PASS=test-password
EMAIL_FROM="Sports Excitement Test <test@test.com>"

# -----------------------------------------------------------------------------
# Server-Sent Events Configuration (Reduced Limits)
# -----------------------------------------------------------------------------
SSE_HEARTBEAT_INTERVAL=10000
SSE_MAX_CONNECTIONS=100
SSE_CONNECTION_TIMEOUT=30000
SSE_RETRY_INTERVAL=1000

# -----------------------------------------------------------------------------
# Memory Monitoring Configuration (Disabled)
# -----------------------------------------------------------------------------
MEMORY_MONITORING_ENABLED=false
MEMORY_MONITOR_INTERVAL=30000
MEMORY_THRESHOLD=90

# -----------------------------------------------------------------------------
# CORS Configuration (Testing)
# -----------------------------------------------------------------------------
CORS_ENABLED=true
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
CORS_CREDENTIALS=true
CORS_METHODS=GET,POST,PUT,DELETE,OPTIONS
CORS_HEADERS=Content-Type,Authorization,X-Requested-With

# -----------------------------------------------------------------------------
# File Upload Configuration (Testing - Smaller Limits)
# -----------------------------------------------------------------------------
UPLOAD_MAX_FILE_SIZE=1048576
UPLOAD_ALLOWED_TYPES=image/jpeg,image/png
UPLOAD_DESTINATION=storage/test-uploads/
UPLOAD_PRESERVE_FILENAME=false

# -----------------------------------------------------------------------------
# Testing Specific Configuration
# -----------------------------------------------------------------------------
# Environment
NODE_ENV=testing

# Node.js Settings (Optimized for testing)
NODE_OPTIONS="--max_old_space_size=1024"

# Performance Settings (Disabled for testing)
CLUSTER_WORKERS=1
ENABLE_COMPRESSION=false
TRUST_PROXY=false

# -----------------------------------------------------------------------------
# External Services (All Disabled for Testing)
# -----------------------------------------------------------------------------
# Webhooks (Disabled)
WEBHOOK_SECRET=test-webhook-secret
WEBHOOK_TIMEOUT=5000

# Analytics (Disabled)
ANALYTICS_ENABLED=false
ANALYTICS_PROVIDER=test

# Monitoring (Disabled)
MONITORING_ENABLED=false
MONITORING_ENDPOINT=https://test-monitoring.com

# -----------------------------------------------------------------------------
# Feature Flags (Testing Configuration)
# -----------------------------------------------------------------------------
FEATURE_REAL_TIME_NOTIFICATIONS=false
FEATURE_ADVANCED_SEARCH=false
FEATURE_FILE_UPLOADS=true
FEATURE_USER_ANALYTICS=false
FEATURE_ADMIN_PANEL=true

# -----------------------------------------------------------------------------
# Cache Configuration (Testing)
# -----------------------------------------------------------------------------
CACHE_DRIVER=redis
CACHE_TTL=300
CACHE_PREFIX=se_test_cache:

# -----------------------------------------------------------------------------
# Queue Configuration (Testing)
# -----------------------------------------------------------------------------
QUEUE_DRIVER=redis
QUEUE_PREFIX=se_test_queue:
QUEUE_RETRY_AFTER=30
QUEUE_BLOCK_FOR=null

# -----------------------------------------------------------------------------
# Test Database Cleanup
# -----------------------------------------------------------------------------
# Automatically clean test database after each test
AUTO_CLEANUP_TEST_DB=true
RESET_DB_BETWEEN_TESTS=true

# Test Data
SEED_TEST_DATA=false
TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=testpassword123
TEST_ADMIN_EMAIL=admin@example.com
TEST_ADMIN_PASSWORD=adminpassword123